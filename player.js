class Player
{
    constructor(id, pos, rot)
    {
        this.id = id;
        this.pos = pos;
        this.rot = rot;
    }

    addKolonialData(kolonial)
    {
        this.kolonial = kolonial;
    }
}